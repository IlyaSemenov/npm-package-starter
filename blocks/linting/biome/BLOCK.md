# linting/biome

Adds Biome linting and formatting.

## Apply

Copy to the project root:

- `biome.template.jsonc` renamed to `biome.jsonc`

Merge into the matching files:

- `.vscode/`
- `lefthook.yml`
- `package.json`
