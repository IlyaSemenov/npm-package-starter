# default first-line-heading is too strict for small instruction files like CLAUDE.md
first-line-heading: false

# default line-length is too strict for 'one sentence per line' formatting
line-length: false

table-column-style:
  style: aligned

table-pipe-style:
  style: leading_only
